export const metadata = { title: 'HUSKY — WORKER, DEV.', description: 'HUSKY digital worker with Arisa AI.' }

export default function RootLayout({ children }) {
  return <html lang="ru"><body>{children}</body></html>
}
